# 由 bootstrap.sh 通过 envsubst 渲染。
# docker-compose.yml (testenv-app project) 直接读取这份 .env。

# ===== 客户端访问 =====
HOST_IP=${HOST_IP}
PORT_OFFSET=${PORT_OFFSET}

# ===== 测试账号 =====
TEST_USER_EMAIL=${TEST_USER_EMAIL}
TEST_USER_PASSWORD=${TEST_USER_PASSWORD}
TEST_USER_USERNAME=${TEST_USER_USERNAME}

# ===== AI 供应商 ===== (backend/config.py 实际只认这 5 个 key)
DEEPSEEK_KEY=${DEEPSEEK_KEY}
GLM_ANTHROPIC_AUTH_TOKEN=${GLM_ANTHROPIC_AUTH_TOKEN}
GLM_ANTHROPIC_BASE_URL=${GLM_ANTHROPIC_BASE_URL}
CC_ANTHROPIC_AUTH_TOKEN=${CC_ANTHROPIC_AUTH_TOKEN}
CC_ANTHROPIC_BASE_URL=${CC_ANTHROPIC_BASE_URL}

# ===== ByteDance ASR（可空）=====
BYTEDANCE_ASR_APP_KEY=${BYTEDANCE_ASR_APP_KEY}
BYTEDANCE_ASR_ACCESS_KEY=${BYTEDANCE_ASR_ACCESS_KEY}
BYTEDANCE_ASR_RESOURCE_ID=${BYTEDANCE_ASR_RESOURCE_ID}

# ===== jsonapp Postgres =====
JSONAPP_DB_USER=jsonapp
JSONAPP_DB_PASSWORD=${JSONAPP_DB_PASSWORD}
JSONAPP_DB_NAME=jsonapp
JSONAPP_DB_PORT=${JSONAPP_DB_PORT}

# ===== backend Redis（AI_SESSION_REDIS_*）=====
BACKEND_REDIS_PASSWORD=${BACKEND_REDIS_PASSWORD}

# ===== App MinIO =====
APP_MINIO_ROOT_USER=${APP_MINIO_ROOT_USER}
APP_MINIO_ROOT_PASSWORD=${APP_MINIO_ROOT_PASSWORD}
APP_MINIO_ACCESS_KEY=${APP_MINIO_ACCESS_KEY}
APP_MINIO_SECRET_KEY=${APP_MINIO_SECRET_KEY}
APP_MINIO_PORT=${APP_MINIO_PORT}
APP_MINIO_CONSOLE_PORT=${APP_MINIO_CONSOLE_PORT}

# ===== Supabase（暴露给主 compose）=====
KONG_HTTP_PORT=${KONG_HTTP_PORT}
KONG_HTTPS_PORT=${KONG_HTTPS_PORT}
JWT_SECRET=${JWT_SECRET}
SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}

# ===== OpenIM =====
OPENIM_SECRET=${OPENIM_SECRET}
OPENIM_WEBHOOK_SECRET=${OPENIM_WEBHOOK_SECRET}
OPENIM_WS_PORT=${OPENIM_WS_PORT}
OPENIM_API_PORT=${OPENIM_API_PORT}
OPENIM_ADMIN_PORT=${OPENIM_ADMIN_PORT}

# ===== Flask / Registry / Config Center =====
FLASK_SECRET_KEY=${FLASK_SECRET_KEY}
REGISTRY_ADMIN_TOKEN=${REGISTRY_ADMIN_TOKEN}
REGISTRY_ADMIN_AUTHOR_EMAIL=${REGISTRY_ADMIN_AUTHOR_EMAIL}
REGISTRY_ADMIN_AUTHOR_NAME=${REGISTRY_ADMIN_AUTHOR_NAME}
REGISTRY_ADMIN_AUTHOR_ID=${REGISTRY_ADMIN_AUTHOR_ID}
BACKEND_PORT=${BACKEND_PORT}
REGISTRY_PORT=${REGISTRY_PORT}
CONFIG_CENTER_PORT=${CONFIG_CENTER_PORT}
CONFIG_CENTER_ADMIN_PASSWORD=${CONFIG_CENTER_ADMIN_PASSWORD}
CONFIG_CENTER_SESSION_SECRET=${CONFIG_CENTER_SESSION_SECRET}

# ===== User Center（Supabase 用户管理后台）=====
USER_CENTER_PORT=${USER_CENTER_PORT}
USER_CENTER_ADMIN_PASSWORD=${USER_CENTER_ADMIN_PASSWORD}
USER_CENTER_SESSION_SECRET=${USER_CENTER_SESSION_SECRET}

# ===== Registry Mirror =====
# 上游 registry 域名（含 scheme，如 https://myapp-registry.dapangyu.work）。
# 留空 = 本实例不开 mirror，独立运行。
REGISTRY_UPSTREAM=${REGISTRY_UPSTREAM}
# 同步间隔（秒），<=0 = 只在启动时同步一次不周期
REGISTRY_MIRROR_SYNC_INTERVAL_SEC=${REGISTRY_MIRROR_SYNC_INTERVAL_SEC}
