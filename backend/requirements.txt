Flask>=3.1.0
flask-socketio>=5.3.0
python-socketio>=5.11.0
websocket-client>=1.8.0
psycopg2-binary>=2.9.0
anthropic>=0.90.0
requests>=2.30.0
python-dotenv>=1.0.0
minio>=7.2.0
aiohttp>=3.8.0
pyaudio>=0.2.11
# 生产 WSGI server：ai-app 用 -k eventlet -w 1（SocketIO + SSE），registry 用 -k gthread -w 4 --threads 8
gunicorn>=21.2.0
eventlet>=0.36.0
# AI session 状态存储（Phase 1: ai-background-push）
# redis-py 5.x 在 eventlet monkey_patch 下走 socket 协程化，不需要单独的 aioredis
redis>=5.0.0
# FCM HTTP v1 API 需要 OAuth access token，从 service-account JSON 换出来
google-auth>=2.30.0
